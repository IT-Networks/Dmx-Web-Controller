{
  "name": "dmx-web-controller",
  "version": "1.0.0",
  "description": "DMX Web Controller - Native Mobile App",
  "author": "IT-Networks",
  "license": "MIT",
  "scripts": {
    "android:build": "npx cap sync android && cd android && ./gradlew assembleRelease",
    "android:build:debug": "npx cap sync android && cd android && ./gradlew assembleDebug",
    "sync": "npx cap sync",
    "add:android": "npx cap add android",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^5.7.0",
    "@capacitor/android": "^5.7.0",
    "@capacitor/splash-screen": "^5.0.7"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0"
  }
}
